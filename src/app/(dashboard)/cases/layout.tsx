import { MobileFieldShell } from "@/components/field/mobile-field-shell";

export default function CasesLayout({ children }: { children: React.ReactNode }) {
  return <MobileFieldShell>{children}</MobileFieldShell>;
}

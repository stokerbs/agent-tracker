# Detective Pulse — Project Backlog

**Purpose.** Single source of truth for all future engineering work. Every item is traceable back to a finding in one of the three source artifacts below. No item here is an invented work item; the only forward-looking entries live in **Future Enhancements** and are each grounded in an existing source finding.

**Source provenance.**
- **Technical Debt Register** (23 classified items, P0–P4) — the originating `Register #N` is cited on every backlog item.
- **Database Knowledge** — `.claude/knowledge/database/{schema,enums,indexes,functions,relationships,rls,triggers}.md`.
- **Architecture Inventory** — read-only survey of `src/` (`app/`, `components/`, `lib/`) + `supabase/`.
- **Project memory** — native app (Capacitor) push activation outstanding item.

**Status legend.**
- `BACKLOG` — captured, not yet scheduled.
- `READY` — fully specified by sources, no unmet dependencies.
- `IN PROGRESS` — a source evidences active work.
- `BLOCKED` — waiting on another unfinished item (blocker named).
- `DONE` — a source evidences completion.

**Sub-task qualifiers** (apply to sub-tasks recorded under a backlog item, never to the backlog item's own `Current Status`):
- `LOCAL DONE` — builder-complete and both gates (security-reviewer + qa-test-engineer) APPROVED, verified locally, but **not yet merged**.
- `PENDING PR` — Merge Status: no git commit/PR exists yet, so the release-manager has deferred final merge authorization until a live PR exists. A `LOCAL DONE` sub-task carries `PENDING PR` until merged.

**Owner-agent legend** (the 15 specialists referenced here):
`data-migration-author`, `backend-api-builder`, `frontend-builder`, `maps-geo-builder`, `native-app-builder`, `ai-engineer`, `security-reviewer`, `performance-engineer`, `qa-test-engineer`, `devops-engineer`, `operations-engineer`.

> Scope note: P0 tier is empty. No source establishes a confirmed, exploitable security or correctness defect. Items that "sound" P0 (the `gps903_session` stale-cookie surface; broad `createServiceClient` reach) are explicitly bounded by the sources as not-user-reachable / no-misuse-asserted and are filed at their evidenced severity (P1) under Security Improvements.

---

## 1. Active Features

The native push activation is the only in-flight feature evidenced by the sources (project memory: Capacitor Phase A+B code shipped via migrations 0068–0069; FCM env + native rebuild outstanding). The push transport code exists in `lib/push/` and `device_tokens`/`gps_tokens` tables exist (migrations 0068–0069), corroborating the Inventory. No other in-flight feature is evidenced; none invented.

| ID | Priority | Title | Owner Agent | Dependencies | Current Status |
|---|---|---|---|---|---|
| AF-1 | P1 | Native app push activation (FCM env + APNs config + native rebuild) — code shipped (migrations 0068–0069, `lib/push/` APNs+FCM), activation outstanding | native-app-builder | none | IN PROGRESS |

---

## 2. Technical Debt

P2 Maintainability and P4 Cleanup items.

| ID | Priority | Title | Owner Agent | Dependencies | Current Status |
|---|---|---|---|---|---|
| TD-1 | P2 | Duplicated Google Maps setup across `map/live-map.tsx` and `gps/gps-monitor-map.tsx` (from Register #8) | maps-geo-builder | TD-3 | BLOCKED — shares battery-helper duplication with TD-3 |
| TD-2 | P2 | GPS903 domain fragmentation across DB, lib, components, routes (from Register #9) | maps-geo-builder | TD-1, TD-4, TD-5 | BLOCKED — depends on TD-4 and TD-5 |
| TD-3 | P2 | Centralized `formatDate`/`formatCurrency`/`batteryColor` helpers bypassed (41 files raw `toLocale*`; ~7 inline `batteryColor`) (from Register #11) | frontend-builder | none | MERGED via PR #18 (f45fd98) — SAFE-SLICE code change: new additive `bangkokDateKey()` helper in `src/lib/utils.ts` consolidating 12 byte-identical en-CA/Bangkok machine-date-key sites + new helper tests `src/lib/utils.test.ts`; en-GB/THB/battery display sites deliberately NOT swept (would change output) — deferred to TD-3b; no commit/PR yet, release-authorized for a `fix/*` PR |
| TD-4 | P2 | `lib/gps903.ts` is a single 797-line integration file (from Register #10) | maps-geo-builder | none | BACKLOG |
| TD-5 | P2 | `gps903_devices` catalog has no FK to `gps_devices`/`gps903_credentials` (app-code-matched) (from Register #12) | data-migration-author | none | BACKLOG |
| TD-6 | P2 | Inconsistent component co-location and test-location conventions (8 components inside `app/`; tests split `lib/` vs `src/__tests__/`) (from Register #13) | frontend-builder | none | BACKLOG |
| TD-7 | P2 | Multiple/ad-hoc action-file split per feature (`actions.ts` + `account-actions.ts` + `intelligence-actions.ts`) (from Register #15) | backend-api-builder | none | BACKLOG |
| TD-8 | P4 | Duplicate index on `case_agents.agent_id` (`case_agents_agent_idx` + `idx_case_agents_agent_id`; neither dropped) (from Register #18) | data-migration-author | none | MERGED via PR #10 (64ceb7f) — DB migration (`supabase/migrations/0072_drop_duplicate_case_agents_agent_index.sql` drops the redundant legacy `case_agents_agent_idx`, keeps `idx_case_agents_agent_id`); no commit/PR yet, release-authorized for a `fix/*` PR; migration not yet applied (devops applies at deploy) |
| TD-9 | P4 | Orphaned `report_status` enum (no column uses it after `reports` dropped in 0051) (from Register #19) | data-migration-author | none | MERGED via PR #11 (fecacf4) — DB migration (`supabase/migrations/0073_drop_orphaned_report_status_enum.sql` drops the orphaned `report_status` enum; no CASCADE); no commit/PR yet, release-authorized for a `fix/*` PR; migration not yet applied (devops applies at deploy; optional post-apply `supabase gen types` to clear the stale generated entry) |
| TD-10 | P4 | Legacy `expense_category` values `food`/`hotel` still defined (data migrated in 0053) (from Register #20) | data-migration-author | none | MERGED via PR #12 (05555b1) — DB migration (`supabase/migrations/0074_remove_legacy_expense_category_values.sql` removes legacy `food`/`hotel` from `expense_category` via a type swap; both consumers — `expenses.category` + `monthly_expense_summary` — handled; safety remap per 0053; no CASCADE); no commit/PR yet, release-authorized for a `fix/*` PR; migration not yet applied (devops applies at deploy after 0073, verify non-prod first; optional post-apply `supabase gen types`) |
| TD-11 | P4 | Empty, untracked `components/reports/` directory (orphaned after 0051) (from Register #21) | frontend-builder | TD-9 | DONE (already-resolved; verify-only) — the orphaned empty `components/reports/` directory is confirmed ABSENT on disk (no `reports/` subdir under `src/components/`; nothing imports `components/reports`; live `src/app/(dashboard)/reports/` route intact); no code change, no PR (Register #21's "directory exists" was a stale earlier-audit snapshot). security-APPROVED, QA-APPROVED, release GO |
| TD-12 | P4 | Thin automated test coverage (4 test files vs 86 app + 130 component files) (from Register #22) | qa-test-engineer | none | BACKLOG |
| TD-13 | P4 | Redundant trigger functions `set_updated_at()` / `touch_updated_at()` (from Register #23) | data-migration-author | none | MERGED via PR #13 (050b4ef) — DB migration (`supabase/migrations/0075_consolidate_updated_at_trigger_functions.sql` drops the redundant `touch_updated_at()`, keeps canonical `set_updated_at()`, repointing its one dependent trigger `ai_prompts_updated_at` first; no CASCADE); no commit/PR yet, release-authorized for a `fix/*` PR; migration not yet applied (devops applies at deploy after 0074) |
| TD-14 | P4 | Post-SEC-2 cleanup: regenerate `src/lib/database.types.ts` to drop the stale `gps903_session` type (line ~1335) + fix the stale `README.md:186` GPS903 session-table reference (still names `gps903_session`; should be `gps903_credential_sessions`) — doc/codegen hygiene residue of the SEC-2 `DROP TABLE` (from SEC-2) | devops-engineer (data-migration-author for the typegen) | SEC-2 (migration must be applied first) | MERGED via PR #20 (489273f) — DONE. Migrations 0070–0075 are now APPLIED to the linked DB; `src/lib/database.types.ts` regenerated (gps903_session/report_status/food·hotel removed; idempotent — verified no schema drift), `README.md:186` fixed (→ `gps903_credential_sessions`), obsolete food/hotel comment removed from `src/lib/types.ts`. security-APPROVED, QA-APPROVED, release GO |
| TD-3b | P2 | [Behavior-CHANGING] Extend centralized formatters (`formatDate`/`formatCurrency`/`batteryColor`) with locale/currency/threshold options and adopt at the en-GB/THB/battery display call sites (the majority of Register #11); CHANGES rendered output for the ~14 existing en-US/USD consumers → needs product + visual sign-off (from TD-3 finding) | frontend-builder | none (product/visual sign-off gated) | BACKLOG — needs a product/design decision before READY (display-output change; not behavior-preserving). Priority P2 — same tier as parent TD-3: this is the bulk of the original Register #11 helper-bypass debt (the en-GB/THB/battery display majority that TD-3's safe slice deliberately left untouched) |

---

## 3. Security Improvements

Security-natured items. Each is filed at its evidenced severity; none is a confirmed exploit.

| ID | Priority | Title | Owner Agent | Dependencies | Current Status |
|---|---|---|---|---|---|
| SEC-1 | P1 | `createServiceClient` (RLS-bypass) reachable from 22 files incl. `page.tsx` — audit Golden Rule 2 holds at every call site (no misuse asserted; breadth/audit risk) (from Register #2) | security-reviewer | none | LOCAL DONE (audit-only; 0 source files; security-APPROVED, QA-APPROVED) — no separate PR (audit artifact `.claude/audits/SEC-1-service-client-rls-audit.md`) |
| SEC-2 | P1 | Superseded-but-undropped `gps903_session` holding a live session cookie (RLS-enabled, zero policies → not user-reachable; stale credential surface) (from Register #1) | data-migration-author | SEC-4 | MERGED via PR #8 (18101e9) — DB migration (`supabase/migrations/0070_drop_gps903_session.sql` drops obsolete `gps903_session` singleton); no commit/PR yet, release-authorized for a `fix/*` PR; migration not yet applied (devops applies at deploy; regenerate `database.types.ts` after apply) |
| SEC-3 | P1 | `can_access_case()` dead-but-live: defined (redefined 0063, assignment-scoped) but wired into no policy/query; do not assume it gates access (from Register #7) | security-reviewer | none | LOCAL DONE (audit-only; 0 source files; security-APPROVED, QA-APPROVED) — no separate PR (audit artifact `.claude/audits/SEC-3-can-access-case-dead-but-live-audit.md`) |
| SEC-4 | P2 | Debug endpoint `api/debug/gps903/route.ts` co-resident with production routes (`CRON_SECRET`-gated, no in-app caller; uses service role) (from Register #14) | devops-engineer | SEC-1 | MERGED via PR #14 (f2eb964) — code change (`src/app/api/debug/gps903/route.ts` prod `VERCEL_ENV` 404 guard + test `src/app/api/debug/gps903/route.test.ts`); no commit/PR yet, release-authorized for a `fix/*` PR |
| SEC-5 | P1 | `notifications.ts` exposes unguarded `"use server"` service-role helpers (`notifyUsers`/`notifyRole`) — move to a `server-only` internal module (no `"use server"`, mirroring `push/send.ts`) or add an auth guard at the top of each helper, then re-audit (from SEC-1 audit FLAG-1) | operations-engineer | none | MERGED via PR #15 (8906ca2) — code change (`src/lib/notifications.ts` "use server" → `import "server-only"` + regression test `src/lib/notifications.test.ts`); no commit/PR yet, release-authorized for a `fix/*` PR |
| SEC-6 | P3 | Missing `audit_logs` coverage on several privileged service-role mutations (credential CRUD, user role/active changes, invoice soft-delete, AI-prompt edits, device-access grants/revokes, geofence create/delete; only OTP `LOGIN` is logged) — add audit-log writes for privileged mutations (Critical Rule 5) (from SEC-1 audit Note D) | backend-api-builder | none | MERGED via PR #16 (5a88351) — code change (new `src/lib/audit.ts` helper + 13 exactly-once audit_logs writes across 5 action files + tests `src/__tests__/audit-logging.test.ts`; free-text action col, no migration; users/actions.ts left to existing profiles trigger to avoid double-log); no commit/PR yet, release-authorized for a `fix/*` PR |

---

## 4. Performance Improvements

P3 items plus the duplicate index's performance dimension (the index itself is tracked as cleanup under TD-8; the query/scan-cost items live here).

| ID | Priority | Title | Owner Agent | Dependencies | Current Status |
|---|---|---|---|---|---|
| PERF-1 | P3 | FK columns without a covering index (~25 FK cols; higher-traffic: `case_messages.sender_id`, case-scoped FKs) (from Register #16) | performance-engineer | none | BACKLOG |
| PERF-2 | P3 | Near-duplicate GPS-device flatten queries in `lib/queries.ts` (`getActiveGpsDevices` / `getGpsMonitorDevices`, lines 251–298) (from Register #17) | backend-api-builder | ARCH-1 | MERGED via PR #17 (4d61dbd) — code change (extracted shared `activeGpsDevicesQuery` helper in `src/lib/queries.ts` deduping the two GPS-device flatten queries; behavior + perf preserved; + test `src/lib/queries.test.ts`); no commit/PR yet, release-authorized for a `refactor/*` (or `fix/*`) PR |

---

## 5. Architecture Improvements

P1 architectural-risk items.

| ID | Priority | Title | Owner Agent | Dependencies | Current Status |
|---|---|---|---|---|---|
| ARCH-1 | P1 | Layering inversion: `lib/queries.ts` (data layer) imports chart view-types from `components/dashboard/charts` (UI layer) (from Register #3) | backend-api-builder | none | MERGED via PR #6 — layering-inversion fix (data layer no longer imports chart view-types from the UI layer); security-APPROVED, QA-APPROVED |
| ARCH-2 | P1 | No `error.tsx` anywhere in `app/`; only one `loading.tsx` (violates Golden Rule 4 across ~25 segments) (from Register #4) | frontend-builder | none | DONE |
| ARCH-3 | P1 | Hand-mirrored `lib/types.ts` (661 lines) drifting from 69 migrations (header recommends generated types) (from Register #5) | data-migration-author | none | LOCAL DONE (audit-only; 0 source files; security-APPROVED, QA-APPROVED) — no separate PR (audit artifact `.claude/audits/ARCH-3-types-drift-audit.md`) |
| ARCH-4 | P1 | `notifications.ts` straddles persistence and transport concerns (DB insert + `sendPushToUsers` fan-out) (from Register #6) | operations-engineer | none | BACKLOG |

**ARCH-2 sub-task progress** (ARCH-2 is **DONE** — all sub-tasks T1.1–T3.2 complete; **Phase 1 (T1.1–T1.6) MERGED via PR #4 (`e35ef01`)**; **Phase 2 (T2.1–T2.7) MERGED via PR #4 (`e35ef01`)**; **Phase 3 (T3.1 coverage verification + T3.2 consistency/leak sweep) both PASS, security-APPROVED, QA-APPROVED, release GO**. Golden Rule 4 fully implemented + coverage-certified + consistency/leak-certified across the App Router tree; ARCH-2 was the last open Phase-3 dependency):

> **Merge reconciliation (2026-06-25):** PR #4 (`feat(arch-2): App Router error & loading boundaries`) was **MERGED into `main`** at merge commit `e35ef01205831070f02ae280c3675786af04bf69` (merged 2026-06-25 13:48Z; Vercel deploy completed). PR #4 carried the full ARCH-2 Phase 1 + Phase 2 implementation (T1.1–T2.7). The prior `PENDING PR — no commit/PR yet` Merge Status on those rows is therefore **stale and has been reconciled to MERGED**. Verified from git: `e35ef01` is HEAD of `main`; `gh pr view 4` → state MERGED. T3.1 is a 0-file verification audit certifying that merged tree (no separate PR of its own).

> **Disk re-verification (2026-06-25):** The full ARCH-2 working tree was re-evaluated from disk as the single source of truth. All 34 boundary/loading files + the shared component are present; every source path cited across all 13 sign-offs (`ARCH-2-T1.1`…`T2.7`) exists on disk; `tsc --noEmit` exits 0 and ESLint reports no warnings or errors across the tree. T2.4 = 4 internal digest-only `error.tsx` (invoices/expenses/payroll/reports), verified sound; T2.6 = deliberate 0-file non-change (the `(auth)` segments rely on the T1.6 `(auth)/error.tsx` group boundary), verified consistent with disk. An earlier status-integrity flag (T2.4/T2.6 "unverified") is superseded by this re-verification and the completion of their review work; all Phase-2 sub-tasks are LOCAL DONE per their sign-offs.

| Sub-task | Title | Owner | Status | Merge Status | Sign-off |
|---|---|---|---|---|---|
| T1.1 | Shared error-state component | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T1.1.md` |
| T1.2 | Boundary error-logging helper `logBoundaryError` (`src/lib/errors.ts`, additive) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T1.2.md` |
| T1.3 | Error-boundary i18n strings (`messages/en.json` + `messages/th.json`, additive 9-key `errorBoundary` block) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T1.3.md` |
| T1.4 | Async/loading-gap audit (read-only; `.claude/audits/ARCH-2-loading-audit.md`) — 38 segments, 9 need `loading.tsx`, 18 → segment `error.tsx` (T2.1–T2.7) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T1.4.md` |
| T1.5 | Root + global error boundary (`src/app/error.tsx` + `src/app/global-error.tsx`, new) — renders T1.1 `ErrorState`, logs via T1.2 `logBoundaryError` (`app:error`/`app:global-error`), only `error.digest` to UI | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T1.5.md` |
| T1.6 | Route-group root boundaries (4 new `error.tsx`: `(dashboard)`/`(auth)` internal+digest; `(portal)/portal`/`(portal-auth)` portal-safe, NO digest — trust boundary enforced) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED [portal leak-free], QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T1.6.md` |
| T2.1 | Cases & Clients segment boundaries (4 new: `cases/[id]/loading.tsx` + `error.tsx`, `clients/[id]/loading.tsx` + `error.tsx`; internal variant, digest-only, loading PII-free; `cases/intake` light → no files) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T2.1.md` |
| T2.2 | Agents/Field/Evidence segment boundaries (4 new: `agents/[id]/loading.tsx` + `error.tsx`, `field/[id]/loading.tsx` + `error.tsx`; internal variant, digest-only, field GPS skeleton leak-free; `evidence` DEFERRED to group boundary per T1.4 audit; list segments → no files) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T2.2.md` |
| T2.3 | GPS & Map segment boundaries (8 new: 5 `error.tsx` `map`/`gps-devices/[id]`/`gps-monitor`/`gps903-discovery`/`gps903-credentials` + 3 skeletons `map`/`gps-monitor`/`gps-devices/[id]`; internal variant, digest-only, no coords/IMEI/credentials/Maps-key; gps903-* error-only, gps-devices index none) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED [no coords/IMEI/credentials/Maps-key], QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T2.3.md` |
| T2.5 | Admin/Ops & Settings segment boundaries (4 new `error.tsx`: `emergency`/`audit`/`users`/`settings/ai-prompts`; internal variant, digest-only, distinct contexts; `users` boundary imports no service-role/admin/secret module; no `loading.tsx` per audit; dashboard-index/settings-root/settings-profile rely on group) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T2.5.md` |
| T2.7 | Portal & Portal-auth segment boundaries (3 new: `(portal)/portal/loading.tsx`, `(portal)/portal/cases/[id]/loading.tsx` + `error.tsx`; portal-safe, NO detail/digest, loading PII-free) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED [portal leak-free], QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T2.7.md` |
| T2.4 | Invoices/Expenses/Payroll/Reports segment boundaries (4 new `error.tsx`: `invoices`/`expenses`/`payroll`/`reports`; internal variant, digest-only, unique log scopes `invoices:error`/`expenses:error`/`payroll:error`/`reports:error`; no `loading.tsx` in scope → rely on group skeleton) | frontend-builder | LOCAL DONE (builder-complete; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T2.4.md` |
| T2.6 | `(auth)` route group deliberate-non-change (`login` = `'use client'`, `login/verify` = async but only `await searchParams` + self-Suspended, `register` = sync redirect, `(auth)/layout.tsx` only `getTranslations`) do no render-time DB work → no segment `error.tsx`/`loading.tsx`; rely on T1.6 `(auth)` group boundary; **0 new files** (per T1.4 audit) | frontend-builder | LOCAL DONE (verification; 0 files; security-APPROVED, QA-APPROVED) | MERGED via PR #4 (`e35ef01`) | `.claude/sign-offs/ARCH-2-T2.6.md` |
| T3.1 | Full Golden-Rule-4 coverage verification pass (read-only; `.claude/audits/ARCH-2-T3.1-coverage-verification.md`; **0 source files changed**) — overall **PASS**: 9/9 `loading.tsx`, 18/18 segment `error.tsx`, 4/4 group-root `error.tsx`, root + `global-error` present; 24 boundaries wired to shared `ErrorState` + `logBoundaryError` with **24 unique log scopes**; trust-boundary holds (21 internal digest, 3 portal no-digest); en/th i18n parity; `evidence`/`(auth)`/`cases/intake` deliberate non-changes confirmed; tsc + lint exit 0 | frontend-builder | LOCAL DONE (verification PASS; security-APPROVED, QA-APPROVED) | Implementation it certifies is MERGED via PR #4 (`e35ef01`); the verification pass itself is a 0-file audit (no separate PR) | `.claude/sign-offs/ARCH-2-T3.1.md` |
| T3.2 | Deeper consistency review + adversarial portal-leak sweep (FINAL ARCH-2 sub-task; `.claude/audits/ARCH-2-T3.2-consistency-leak-sweep.md`; **0 source files changed**) — overall **PASS**. Half A: 7/7 consistency dimensions PASS (two T1.4 Medium-confidence skeleton-fidelity flags — `gps-monitor`, `map` — CLOSED as faithful map+rail mirrors; one acceptable-as-is log-scope singular/plural stem irregularity recorded, deliberately NOT renamed to preserve T3.1's certified 24-unique-scope ledger). Half B: trust-boundary verdict — **NO leak under any render path** (8 negative paths explicitly ruled out); `ErrorState` `showDetail` hard-guard intact. tsc + lint exit 0 | frontend-builder | LOCAL DONE (verification PASS; 0 files; security-APPROVED, QA-APPROVED) | Certified implementation MERGED via PR #4 (`e35ef01`); verification pass itself is a 0-file audit (no separate PR) | `.claude/sign-offs/ARCH-2-T3.2.md` |

Dependency note (ARCH-2 breakdown): T1.2/T1.3/T1.4 depend on nothing; T1.5 & T1.6 depend on T1.1 **AND** T1.2 **AND** T1.3; Phase 2 (T2.x) depends on T1.4+T1.5+T1.6. Phase 1 + Phase 2 (T1.1–T2.7) are **MERGED via PR #4 (`e35ef01`)** — see the merge reconciliation note above (typecheck + lint clean; all sign-offs present and file-consistent). Phase 3: **T3.1 (full Golden-Rule-4 coverage verification pass) is LOCAL DONE — overall PASS, security-APPROVED, QA-APPROVED, release GO retrospective** (depended on T2.1–T2.7, all merged). **T3.2 (deeper consistency review + adversarial portal-leak sweep) is now LOCAL DONE — overall PASS (0 source files), security-APPROVED [trust boundary cannot leak under any render path], QA-APPROVED [Half-A consistency + T3.1 ledger unchanged], release GO** (its dependencies T2.7 + T3.1 are both satisfied). **All ARCH-2 sub-tasks T1.1–T3.2 are now complete; Phase 3 is done; ARCH-2 `Current Status` is flipped to DONE** — ARCH-2 was the last open Phase-3 dependency. (Non-change of record: the `evidence` segment's error/loading was deliberately DEFERRED to the T1.6 group boundary per the T1.4 audit — intentional, not an omission.) (Carried observation: T1.5 `global-error.tsx` copy is English under a possibly-`th` locale — deliberate provider-independence trade-off, tracked in its sign-off. T3.2 acceptable-as-is record: the log-scope singular/plural stem irregularity was deliberately NOT renamed, to preserve the security-load-bearing 24-unique-scope ledger certified in T3.1.)

> **Process flags (release-manager, non-blocking, reinforced going forward):** (1) **Merge-after-gate ordering:** PR #4 was merged into `main` *before* the T3.1 release-gate decision was rendered (the GO is therefore a *retrospective* certification). The documented pipeline requires Security + QA + release GO **before** merge; future ARCH-2/feature merges must await the release-gate verdict first. (2) **Branch-naming drift:** the merged branch used `feat/*` (and sibling `feat/arch-3`) whereas `PROJECT_RULES.md` documents `feature/*`; cosmetic, non-blocking — align naming or update the rule.

> GPS903 fragmentation (Register #9 → TD-2) and map-rendering duplication (Register #8 → TD-1) are architectural in nature but are filed once under Technical Debt to keep each Register item in a single section; their IDs remain traceable there.

---

## 6. Future Enhancements

Forward-looking only. Each is clearly implied by an existing source finding and is the directional counterpart to a debt item; none is invented beyond that.

| ID | Priority | Title | Owner Agent | Dependencies | Current Status |
|---|---|---|---|---|---|
| FUT-1 | P1 | [Forward-looking → implemented] Adopt generated Supabase DB types to replace hand-mirrored `lib/types.ts` (directional counterpart to ARCH-3 / Register #5; the file's own header recommends `supabase gen types`). Cutover implemented: NEW CLI-generated `src/lib/database.types.ts` (`supabase gen types typescript --linked --schema public`, CLI 2.106.0) + `src/lib/types.ts` rewritten as a thin alias surface (`Row<T>`/`Enums<T>`) over the generated `Database`; all exported names preserved, 98 importers unchanged. The ARCH-3 drift audit (`.claude/audits/ARCH-3-types-drift-audit.md`) was the input/remediation plan: must-survive non-table-mirror set (Intake* block, composed/enriched, server-decorated fields), class-C narrowing unions, and the opaque-credential boundary (`device_password`/`*_enc`) were all honored. DB-side `ExpenseCategory` `food`/`hotel` re-widening (the generated enum is DB-faithful) remains TD-10 | data-migration-author | ARCH-3 | MERGED via PR #7 — generated-Supabase-types cutover (CLI-generated `src/lib/database.types.ts` + thin `src/lib/types.ts` alias surface); security-APPROVED, QA-APPROVED (per sign-off `.claude/sign-offs/FUT-1.md`) |
| FUT-2 | P2 | [Forward-looking] Consolidate the GPS903 domain (DB + `lib/gps903.ts` + `map/`,`gps/`,`gps903/` + 4 app segments) under one boundary (directional counterpart to TD-2 / Register #9) | maps-geo-builder | TD-2, TD-4 | BACKLOG |
| FUT-3 | P1 | [Forward-looking] Adopt `can_access_case()` as the central case-access guard across RLS/queries (directional counterpart to SEC-3 / Register #7; helper is defined and assignment-scoped, currently unwired) | security-reviewer | SEC-3 | MERGED via PR #9 (bfc85c7) — DB migration (`supabase/migrations/0071_adopt_can_access_case_timeline.sql` wires `can_access_case()` into the two assignment-scoped `timeline_entries` member policies; behavior-identical) + readiness artifact `.claude/audits/FUT-3-adoption-readiness.md`; central adoption on cases/evidence/case_messages deferred to FUT-3b (behavior-changing); migration APPLIED to the linked DB and verified live (0071); the prod-promote **RLS smoke matrix PASSED (2026-06-26)** via impersonated live-DB checks (`SET LOCAL ROLE authenticated` + JWT claims, read-only/rolled back) — assigned member SELECT + INSERT allowed; admin sees all incl. soft-deleted (39) while members see active-only (34, soft-deleted hidden); non-assigned non-admin blocked (0 rows, `can_access_case()`=false; supervisor-equivalent path, no supervisor profiles exist); self-attribution enforced (foreign `agent_id` insert rejected) — confirming behavior-identity on the live schema |
| FUT-4 | P4 | [Forward-looking] Author an ERD / schema README for the 69-migration DB (no schema overview exists; grounded in Inventory + DB-docs "missing documentation" findings) | data-migration-author | none | BACKLOG |
| FUT-3b | P1 | [Forward-looking] Behavior-CHANGING central adoption of `can_access_case()` — migrate the blanket `is_staff()`/blanket-supervisor read guards on `cases`/`evidence`/`case_messages` (and analogous `target_relationships`/`invoices`) to assignment-scoped access; removes non-assigned supervisor read access, so requires product sign-off + supervisor-flow QA + RLS regression evidence (from FUT-3 readiness artifact `.claude/audits/FUT-3-adoption-readiness.md` CORE FINDING) | security-reviewer | FUT-3 | BACKLOG — needs a product decision before READY (access-model change; not behavior-preserving) |

---

## Summary

**Per section:** Active Features 1 · Technical Debt 15 · Security Improvements 6 · Performance Improvements 2 · Architecture Improvements 4 · Future Enhancements 5 — **33 items total** (23 Register items + AF-1 from project memory + FUT-1..FUT-4 forward-looking, of which FUT-1/FUT-2/FUT-3 are directional counterparts to existing debt and FUT-4 is grounded in the documentation-gap findings + SEC-5/SEC-6, two follow-ups grounded in the completed SEC-1 audit — FLAG-1 and Note D respectively, traceable to Register #2's audit deliverable + TD-14, a P4 cleanup follow-up grounded in the completed SEC-2 migration — doc/codegen hygiene residue of the `gps903_session` DROP + FUT-3b, a forward-looking follow-up grounded in the completed FUT-3 readiness artifact — the deferred behavior-CHANGING central adoption of `can_access_case()` + TD-3b, a P2 follow-up grounded in the TD-3 finding — the deferred behavior-CHANGING en-GB/THB/battery display adoption of the centralized formatters, the bulk of Register #11).

**Per status:** BACKLOG 11 · READY 0 (the READY tier is now EMPTY — all READY items have been worked through this session) · IN PROGRESS 1 · BLOCKED 2 · DONE 2 (ARCH-2, TD-11) · MERGED 14 (all merged to `main` via GitHub PRs — ARCH-1 #6, FUT-1 #7, SEC-2 #8, FUT-3 #9, TD-8 #10, TD-9 #11, TD-10 #12, TD-13 #13, SEC-4 #14, SEC-5 #15, SEC-6 #16, PERF-2 #17, TD-3 #18, TD-14 #20; the former `LOCAL DONE / PENDING PR` bucket is now **0**; migrations 0070–0075 APPLIED to the linked DB and **VERIFIED LIVE (2026-06-26)** via `pg_catalog` introspection — every object confirmed in its expected end state: `gps903_session` table dropped + `gps903_credential_sessions` present (0070), 2 `timeline_entries` policies use `can_access_case()` (0071), `case_agents_agent_idx` dropped / `idx_case_agents_agent_id` kept (0072), `report_status` enum dropped (0073), `expense_category` = `[fuel, toll, parking, misc, meals, accommodation, transportation, office]` with `food`/`hotel` absent (0074), `touch_updated_at()` dropped / `ai_prompts_updated_at` repointed to `set_updated_at` (0075); `database.types.ts` regenerated idempotent with no schema drift (TD-14)) · LOCAL DONE / no-PR (audit-only) 3 (ARCH-3, SEC-1, SEC-3) — **33 total**. (ARCH-2 is DONE — all sub-tasks T1.1–T3.2 complete; Phase 1 + Phase 2 MERGED via PR #4 (`e35ef01`), Phase 3 verification passes T3.1 + T3.2 both PASS/security-APPROVED/QA-APPROVED/release GO. The only remaining IN PROGRESS item is AF-1, native push activation. **ARCH-1 (2026-06-25) is LOCAL DONE / PENDING PR** — builder-complete, security-APPROVED, QA-APPROVED, release-authorized for a `feature/*` PR but no commit/PR yet; following the same convention used for ARCH-2's sub-tasks, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain backlog statuses (so it leaves the READY tally and is listed on its own line rather than under READY/DONE). **PERF-2 moved BLOCKED → READY** now that its sole blocker (ARCH-1) is cleared. **ARCH-3 (2026-06-25) is now LOCAL DONE / no-PR (audit-only)** — its deliverable is the read-only drift audit `.claude/audits/ARCH-3-types-drift-audit.md` (0 source files changed), security-APPROVED + QA-APPROVED + release GO; like ARCH-1 it leaves the READY tally and is tracked on its own qualifier line rather than folded into DONE (no merge/PR — the audit artifact lives under the untracked-but-not-gitignored `.claude/`). **FUT-1 (2026-06-25) is now LOCAL DONE / PENDING PR** — the ARCH-3 → FUT-1 generated-types arc is complete: the cutover is implemented (NEW CLI-generated `src/lib/database.types.ts` + `src/lib/types.ts` alias surface), builder-complete, security-APPROVED, QA-APPROVED, release-authorized for a `feature/*` PR but no commit/PR yet; tracked on the same `LOCAL DONE / PENDING PR` qualifier line as ARCH-1, so it leaves the READY tally (FUT-1 had earlier moved BACKLOG → READY once ARCH-3 cleared, then READY → LOCAL DONE / PENDING PR on this run). Net effect on plain counts: READY 9→8 (FUT-1 out to its qualifier line). **SEC-1 (2026-06-25) is now LOCAL DONE / no-PR (audit-only)** — its deliverable is the read-only RLS-bypass audit `.claude/audits/SEC-1-service-client-rls-audit.md` (0 source files changed; 38 invocation sites = 36 PASS + 2 FLAG sites / 1 finding, + 1 type-only N/A), security-APPROVED + QA-APPROVED + release GO; like ARCH-3 it leaves the READY tally and is tracked on its own qualifier line rather than folded into DONE (no merge/PR — audit artifact under `.claude/`). **SEC-4 moved BLOCKED → READY** now that its blocker (the SEC-1 service-role audit it overlapped) is complete; SEC-2 remains BLOCKED on SEC-4 (not yet started). Two new follow-ups were filed from the SEC-1 audit: **SEC-5** (P1, READY — FLAG-1: unguarded `notifications.ts` `"use server"` service-role helpers) and **SEC-6** (P3, READY — Note D: missing `audit_logs` coverage on privileged mutations). Net effect on plain counts vs the prior run: READY 8→10 (SEC-1 out −1, SEC-4 in +1, SEC-5 +1, SEC-6 +1), BLOCKED 5→4 (SEC-4 out), LOCAL DONE / no-PR 1→2 (SEC-1 in); section/total counts 28→30 (SEC-5, SEC-6 added). Sub-task qualifiers are tracked separately from backlog-item status and are not folded into the plain-status counts. **SEC-3 (2026-06-25) is now LOCAL DONE / no-PR (audit-only)** — its deliverable is the read-only dead-but-live audit `.claude/audits/SEC-3-can-access-case-dead-but-live-audit.md` (0 source files changed; verdict CONFIRMED dead-but-live — `can_access_case()` is defined (0002 + assignment-scoped redefinition in 0063) but wired into no policy/query, so it is behaviorally inert; NO FLAGs), security-APPROVED + QA-APPROVED + release GO; like ARCH-3 and SEC-1 it leaves the READY tally and is tracked on its own qualifier line rather than folded into DONE (no merge/PR — audit artifact under `.claude/`). **FUT-3 moved BACKLOG → READY** now that its sole blocker (SEC-3) is cleared — the audit confirms the helper is unwired/inert, so adopting it as the central case-access guard can safely proceed; no new follow-up was filed (SEC-3 produced no FLAGs). Net effect on plain counts vs the prior run: READY unchanged at 10 (SEC-3 out −1, FUT-3 in +1), BACKLOG 10→9 (FUT-3 out), LOCAL DONE / no-PR 2→3 (SEC-3 in); section/total counts unchanged at 30. **SEC-5 (2026-06-25) is now LOCAL DONE / PENDING PR** — the SEC-1 audit FLAG-1 remediation is a real code change (`src/lib/notifications.ts` directive swap `"use server"` → `import "server-only"`, helper bodies unchanged, mirroring `push/send.ts`; + new regression test `src/lib/notifications.test.ts`), builder-complete, security-APPROVED (FLAG-1 closed, callers safe), QA-APPROVED (new test 2/2; full suite 5 files/126 tests pass; tsc exit 0; lint clean), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/sec-5-notifications-server-only`). Following the same convention as ARCH-1/FUT-1, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so SEC-5 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). SEC-5 produced NO follow-up FLAGs of its own and had no dependents to unblock. Net effect on plain counts vs the prior run: READY 10→9 (SEC-5 out), LOCAL DONE / PENDING PR 2→3 (SEC-5 in: ARCH-1, FUT-1, SEC-5); section/total counts unchanged at 30 (SEC-5 already existed as an item). **SEC-4 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #14 debug-endpoint co-residency remediation is a real code change: `src/app/api/debug/gps903/route.ts` gains a production guard (`VERCEL_ENV === "production"` returns 404 as the first statements in GET, before the `CRON_SECRET` check), so the service-role diagnostic 404s in production while staying functional in preview/dev; + new hermetic test `src/app/api/debug/gps903/route.test.ts` (3 cases: prod→404, dev→401, preview→401). Builder-complete, security-APPROVED (one LOW/informational note: `VERCEL_ENV` fail-open only under hypothetical non-Vercel self-hosting — non-blocking, app is Vercel-only and `CRON_SECRET` still fail-closes), QA-APPROVED (SEC-4 test 3/3; full suite 6 files/129 tests pass; tsc exit 0; lint clean), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/sec-4-debug-endpoint-prod-guard`). Following the ARCH-1/FUT-1/SEC-5 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so SEC-4 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). **SEC-2 moved BLOCKED → READY** — SEC-4 (its sole dependency) is now complete, mechanically clearing the dependency; and the substantive "no code path reads `gps903_session`" confirmation, though not itself part of SEC-4's deliverable (SEC-4 guarded the debug endpoint, which reads the replacement `gps903_credential_sessions` table, not the superseded singleton `gps903_session`), is independently satisfied — the only remaining `gps903_session` reference in `src/` is the generated type at `database.types.ts` (no query/read), the singleton having been superseded since migration 0041. SEC-4 produced NO follow-up FLAGs of its own; no new backlog items were filed. Net effect on plain counts vs the prior run: READY unchanged at 9 (SEC-4 out −1, SEC-2 in +1), BLOCKED 4→3 (SEC-2 out), LOCAL DONE / PENDING PR 3→4 (SEC-4 in: ARCH-1, FUT-1, SEC-5, SEC-4); section/total counts unchanged at 30. **SEC-2 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #1 stale-credential-surface remediation is a real DB migration: NEW `supabase/migrations/0070_drop_gps903_session.sql` (`BEGIN; DROP TABLE IF EXISTS public.gps903_session; COMMIT;` + header comment) drops the obsolete singleton superseded since 0041 by `gps903_credential_sessions`; smallest safe change, no CASCADE, replacement table untouched, generated type NOT hand-edited. feature-planner independently verified the table is genuinely unused (DROP recommended); builder-complete, security-APPROVED (all 6 checks — strictly surface-reducing, no auth/session regression, no orphaned references), QA-APPROVED (all 7 checks — idempotent, forward-only, zero broken references; full suite 6 files/129 tests pass; tsc exit 0; lint clean), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/sec-2-drop-gps903-session`); migration NOT yet applied (devops applies at deploy; type regeneration runs AFTER apply). Following the ARCH-1/FUT-1/SEC-5/SEC-4 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so SEC-2 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). SEC-2 produced NO follow-up FLAGs of its own and had no downstream dependents to unblock. One P4 cleanup follow-up was filed: **TD-14** (BLOCKED — regenerate `database.types.ts` post-apply to drop the stale `gps903_session` type + fix the stale `README.md:186` GPS903 session-table reference; doc/codegen hygiene residue of the DROP, gated on the 0070 migration being applied, though the README doc fix can proceed independently). Net effect on plain counts vs the prior run: READY 9→8 (SEC-2 out to its qualifier line), LOCAL DONE / PENDING PR 4→5 (SEC-2 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2), BLOCKED 3→4 (TD-14 in); section count Technical Debt 13→14, total 30→31 (TD-14 added). **SEC-6 (2026-06-26) is now LOCAL DONE / PENDING PR** — the SEC-1 audit Note D remediation is a real code change: NEW `src/lib/audit.ts` helper (`logAudit`, single non-fatal `audit_logs` insert) + exactly-one `logAudit` call added to 13 in-scope privileged service-role mutations across 5 action files (gps903-credentials: CREDENTIAL_CREATE/UPDATE/DELETE/TOGGLE_ACTIVE; invoices: INVOICE_SOFT_DELETE; ai-prompts: AI_PROMPT_UPDATE/RESET/RESTORE; gps-devices: DEVICE_ACCESS_GRANT/REVOKE, DEVICE_RELINK_AGENT; map: GEOFENCE_CREATE/DELETE) + NEW tests `src/__tests__/audit-logging.test.ts` (11). The `action` column is free-text so NO migration was needed; `users/actions.ts` was deliberately left untouched because the `trg_audit_profiles` trigger already audits profile (role/active) changes — avoiding a double-log. No secrets logged in metadata; no RLS/auth/business-logic change. Builder-complete, security-APPROVED (all 7 checks — user role/active trigger-coverage ratified audited-not-gap, no secret in metadata, no duplicate events), QA-APPROVED (all 7 checks — behavior additive/unchanged, exactly-once, lint clean, tsc exit 0, full suite 7 files/140 tests pass, scope clean), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/sec-6-audit-log-coverage`). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so SEC-6 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). SEC-6 produced NO follow-up FLAGs of its own and had no downstream dependents to unblock. Net effect on plain counts vs the prior run: READY 8→7 (SEC-6 out to its qualifier line), LOCAL DONE / PENDING PR 5→6 (SEC-6 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6); section/total counts unchanged at 31 (SEC-6 already existed as an item). **FUT-3 (2026-06-26) is now LOCAL DONE / PENDING PR** — the SEC-3 → FUT-3 `can_access_case()` arc reaches its behavior-preserving conclusion: the USER-DECIDED scope was TIMELINE-ONLY (the minimal behavior-identical change), since feature-planner found that TRUE central adoption on `cases`/`evidence`/`case_messages` is behavior-CHANGING (the live guards are blanket `is_staff()`, broader than the assignment-scoped helper) and out of a behavior-preserving task; `timeline_entries` R6/R7 were the ONLY exact-match (already admin-OR-assigned) safe target. The deliverable is a real DB migration `supabase/migrations/0071_adopt_can_access_case_timeline.sql` (rewrites exactly two policies — `timeline_case_member_select`, `timeline_case_member_insert` — to call `public.can_access_case(case_id)`, preserving the `deleted_at` and self-attribution guards verbatim; helper body unchanged) + readiness artifact `.claude/audits/FUT-3-adoption-readiness.md` (full case-access inventory + SAFE/BEHAVIOR-CHANGING/OUT-OF-SCOPE classification). Builder-complete; security-APPROVED via INDEPENDENT review (different reviewer instance from the implementer; all 7 checks — proven behavior-identical: redundant `is_admin()` via `timeline_admin_all`, SECURITY DEFINER coincides with the inline RLS-subject subquery, non-assigned supervisors still blocked, no other policy/grant/RPC touched); QA-APPROVED (all 9 checks — behavior-identity holds, scope clean, lint clean, tsc exit 0, full suite 7 files/140 tests pass; no RLS test harness in repo → documented, with a recommended post-apply non-prod RLS smoke matrix); release GO. No commit/PR yet, release-authorized for a `feat/*` PR (recommended branch `feat/fut-3-can-access-case-timeline`); migration NOT yet applied (devops applies at deploy; the non-prod RLS smoke matrix is the prod-promote gate). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2/SEC-6 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so FUT-3 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). One forward-looking follow-up was filed: **FUT-3b** (P1, BACKLOG — the deferred behavior-CHANGING central adoption of `can_access_case()` on `cases`/`evidence`/`case_messages` and analogous `target_relationships`/`invoices`, product-gated; needs product sign-off before READY). FUT-3 produced NO follow-up FLAGs of its own and had no downstream dependents to unblock. Net effect on plain counts vs the prior run: READY 7→6 (FUT-3 out to its qualifier line), LOCAL DONE / PENDING PR 6→7 (FUT-3 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6, FUT-3), BACKLOG 9→10 (FUT-3b in); section Future Enhancements 4→5, total 31→32 (FUT-3b added). **PERF-2 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #17 near-duplicate GPS-device flatten queries are deduped by a behavior-preserving Option-A skeleton extraction: feature-planner found the two functions are NOT pure duplicates (different `select` strings + distinct flatten outputs — `getActiveGpsDevices` intentionally leaves `cred_*` undefined, which `LiveMap` relies on via a `??` fallback), so only the identical query skeleton was extracted into a private helper `activeGpsDevicesQuery(supabase, select)` in `src/lib/queries.ts`; both `getActiveGpsDevices` and `getGpsMonitorDevices` route through it with their UNCHANGED select strings + UNCHANGED distinct flatten transforms (`flattenGpsRow` exported for testing), callers untouched, no behavior change and no perf change; + new test `src/lib/queries.test.ts` (2 tests). Builder-complete, security-APPROVED (5/5 — RLS `createClient` preserved not service-role; select/filters/order byte-identical; no new secret exposure; no injection surface; scope clean), QA-APPROVED (9/9 — behavior/shape identical; `cred_*` still undefined on `getActiveGpsDevices`; callers unaffected; test valid+hermetic; lint clean; tsc exit 0; full suite 8 files/142 tests pass; no perf regression), release GO; no commit/PR yet, release-authorized for a `refactor/*` (or `fix/*`) PR (recommended branch `refactor/perf-2-gps-device-query-dedup`). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2/SEC-6/FUT-3 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so PERF-2 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). PERF-2 produced NO follow-up FLAGs of its own and had no downstream dependents to unblock. Net effect on plain counts vs the prior run: READY 6→5 (PERF-2 out to its qualifier line), LOCAL DONE / PENDING PR 7→8 (PERF-2 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6, FUT-3, PERF-2); section/total counts unchanged at 32 (PERF-2 already existed as an item). **TD-3 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #11 helper-bypass debt is closed at its USER-DECIDED SAFE-SLICE scope: feature-planner found that TD-3 as written (use the centralized helpers everywhere) is NOT byte-identical safe — the helpers are en-US/USD with battery thresholds 15/35 while the ~14 existing call sites are en-GB/THB with different thresholds, so a 41-file sweep would CHANGE rendered output (≈0 byte-identical replacements); the only genuinely byte-identical duplication is the machine date-key pattern `toLocaleDateString("en-CA",{timeZone:"Asia/Bangkok"})`. The deliverable is a real code change: a NEW additive helper `bangkokDateKey(date = new Date())` in `src/lib/utils.ts` consolidating **12 byte-identical** en-CA/Bangkok machine-date-key sites (2 not-byte-identical candidates correctly SKIPPED — `timeline-client.tsx:59` had no timezone; `expenses/page.tsx:66` used a different method/locale); no existing helper body changed; zero rendered-output change. The helpers had ZERO unit tests, so a NEW `src/lib/utils.test.ts` (11 tests) was added pinning `formatDate`/`formatCurrency`/`batteryColor`/`bangkokDateKey`. Builder-complete, security-APPROVED (5/5, 0 findings — byte-identity for all 12 incl. the date-arg site, additive bodies, no display change, skips correct, no auth/secret surface, scope clean), QA-APPROVED (8/8 — behavior identical, tests valid+hermetic+pinning, lint clean, tsc exit 0, full suite **9 files/153 tests pass**, scope clean, Golden Rule 4 states unaffected), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/td-3-bangkok-date-key`). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2/SEC-6/FUT-3/PERF-2 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so TD-3 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). One follow-up was filed: **TD-3b** (P2, BACKLOG — the deferred behavior-CHANGING extension of the centralized formatters with locale/currency/threshold options + adoption at the en-GB/THB/battery display sites, the bulk of Register #11; product/visual sign-off-gated; needs a product/design decision before READY). TD-3 produced NO follow-up FLAGs of its own and had no downstream dependents to unblock (TD-1's dependency on TD-3 is unchanged — the battery-helper sweep TD-1 shares was deliberately deferred to TD-3b, so TD-1 stays BLOCKED). Net effect on plain counts vs the prior run: READY 5→4 (TD-3 out to its qualifier line), LOCAL DONE / PENDING PR 8→9 (TD-3 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6, FUT-3, PERF-2, TD-3), BACKLOG 10→11 (TD-3b in); section Technical Debt 14→15, total 32→33 (TD-3b added). **TD-8 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #18 duplicate-index cleanup is a real DB migration: NEW `supabase/migrations/0072_drop_duplicate_case_agents_agent_index.sql` (`BEGIN; DROP INDEX IF EXISTS public.case_agents_agent_idx; COMMIT;` + header comment) drops the redundant legacy `case_agents_agent_idx` (migration 0001) and keeps `idx_case_agents_agent_id` (migration 0048, modern convention + documented covering index for the `case_agents.agent_id` RLS join sub-selects); both were identical non-unique btree indexes on the same column, neither backing a PK/UNIQUE/FK, so a plain `DROP INDEX` is sufficient (no CASCADE), `IF EXISTS` keeps it idempotent, no perf regression (an equivalent index remains), smallest safe change, generated types untouched (indexes not modeled). feature-planner confirmed the duplicate and recommended keeping `idx_case_agents_agent_id`; builder-complete, security-APPROVED (5/5 — no RLS/authorization impact, no constraint impact, no secret exposure, correct redundant index dropped, scope clean), QA-APPROVED (9/9 — migration correct/idempotent, exactly one index remains, forward-only convention, zero broken references, no app/type change, lint clean, tsc exit 0, full suite 9 files/153 tests pass, scope clean), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/td-8-drop-duplicate-case-agents-index`); migration NOT yet applied (devops applies at deploy after 0071; no type regen needed). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2/SEC-6/FUT-3/PERF-2/TD-3 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so TD-8 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). TD-8 produced NO follow-up FLAGs of its own and had no downstream dependents to unblock; one optional NON-blocking doc-hygiene note was recorded (post-merge, `.claude/knowledge/database/indexes.md` could be updated to reflect the dropped index). Net effect on plain counts vs the prior run: READY 4→3 (TD-8 out to its qualifier line), LOCAL DONE / PENDING PR 9→10 (TD-8 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6, FUT-3, PERF-2, TD-3, TD-8); section/total counts unchanged at 33 (TD-8 already existed as an item; no new backlog items — TD-8 produced no FLAGs). **TD-9 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #19 orphaned-enum cleanup is a real DB migration: NEW `supabase/migrations/0073_drop_orphaned_report_status_enum.sql` (`BEGIN; DROP TYPE IF EXISTS public.report_status; COMMIT;` + header comment) drops the orphaned `report_status` enum (created 0001, used only by `reports.status`; the `reports`/`report_versions` tables were dropped in 0051 and the related notify fn/trigger in 0019). feature-planner independently confirmed the enum is genuinely orphaned — NO surviving column/function/cast/trigger/view/later-migration dependency — and recommended a plain `DROP TYPE` (no CASCADE); `IF EXISTS` keeps it idempotent, smallest safe change, generated types untouched (the stale `report_status` entry in `src/lib/database.types.ts` is a harmless static string-literal union, non-breaking, cleared on the next `supabase gen types`). Builder-complete, security-APPROVED (5/5 — no RLS/authorization impact, enum genuinely orphaned, plain DROP without CASCADE correct, no secret exposure, scope clean), QA-APPROVED (8/8 — migration correct/idempotent, zero live references, tsc exit 0 [stale generated entry non-breaking], forward-only convention, no app/type change, lint clean, full suite 9 files/153 tests pass, scope clean; no migration-test harness so none invented), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/td-9-drop-orphaned-report-status-enum`); migration NOT yet applied (devops applies at deploy after 0072; optional post-apply `supabase gen types` to clear the stale generated entry). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2/SEC-6/FUT-3/PERF-2/TD-3/TD-8 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so TD-9 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). TD-9 produced NO follow-up FLAGs of its own; no new backlog items were filed. **TD-11 moved BLOCKED → READY** — its sole dependency TD-9 (the orphaned `report_status` enum drop, which was TD-11's blocker via the shared dropped-`reports` lineage) is now satisfied; TD-11 (empty, untracked `components/reports/` directory, a frontend-builder cleanup) is now READY but NOT started. Net effect on plain counts vs the prior run: LOCAL DONE / PENDING PR 10→11 (TD-9 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6, FUT-3, PERF-2, TD-3, TD-8, TD-9); READY unchanged at 3 (TD-9 out −1 to its qualifier line, TD-11 in +1 from BLOCKED); BLOCKED 4→3 (TD-11 out); section/total counts unchanged at 33 (TD-9 already existed as an item; no new backlog items — TD-9 produced no FLAGs). **TD-10 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #20 legacy-enum-value cleanup is a real DB migration: NEW `supabase/migrations/0074_remove_legacy_expense_category_values.sql` removes the legacy `food`/`hotel` labels from the `expense_category` enum via a full type swap (Postgres cannot `DROP VALUE`). feature-planner identified the enum has TWO consumers — the `expenses.category` column and the `monthly_expense_summary(date)` function — so the migration: applies a self-protecting safety remap (`food`→`meals`, `hotel`→`accommodation`, per 0053) before any cast; creates `expense_category_new` with the 8 surviving values (no `food`/`hotel`); drops the column default and drops the function; swaps `expenses.category` via a text round-trip cast; re-adds the `'misc'` default; drops the old type and renames the new; and recreates `monthly_expense_summary` VERBATIM from 0002. Transactional, no CASCADE; no app/type change. Builder-complete, security-APPROVED (6/6 — recreated SECURITY DEFINER function retains `set search_path = public` + byte-identical body and no grant change; enum removes 2 dead labels and broadens nothing; safety remap precedes cast → no data loss; no expenses RLS policy references the removed labels; no secrets; no CASCADE; scope clean), QA-APPROVED (9/9 — value set correct/ordered, both consumers handled, function diff vs 0002 IDENTICAL, data-safe ordering, NOT NULL + `'misc'` default preserved, tsc exit 0, lint clean, full suite 9 files/153 tests pass, scope clean; no migration-test harness so none invented), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/td-10-remove-legacy-expense-category-values`); migration NOT yet applied (devops applies at deploy after 0073, verify non-prod first — `food`/`hotel` gone, expenses data intact, `monthly_expense_summary` works; optional non-blocking post-apply `supabase gen types` to clear the stale `food`/`hotel` labels from `database.types.ts`). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2/SEC-6/FUT-3/PERF-2/TD-3/TD-8/TD-9 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so TD-10 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). TD-10 produced NO follow-up FLAGs of its own and has no downstream dependents to unblock (FUT-1 merely MENTIONS TD-10 as owning the `ExpenseCategory` re-narrowing — descriptive context, not a dependency; no item is gated on TD-10). Net effect on plain counts vs the prior run: READY 3→2 (TD-10 out to its qualifier line), LOCAL DONE / PENDING PR 11→12 (TD-10 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6, FUT-3, PERF-2, TD-3, TD-8, TD-9, TD-10); section/total counts unchanged at 33 (TD-10 already existed as an item; no new backlog items — TD-10 produced no FLAGs). **TD-11 (2026-06-26) is now DONE (already-resolved; verify-only)** — the Register #21 cleanup is a confirmed NO-OP: the orphaned empty `components/reports/` directory does NOT exist on disk (`src/components/` has ~25 subdirs, NONE named `reports`; nothing imports `components/reports`; the live `src/app/(dashboard)/reports/` route is separate and intact). Git does not track empty directories, so the untracked empty dir was already cleaned / never persisted — Register #21's "directory exists" was a stale earlier-audit snapshot. feature-planner authoritatively determined no directory to delete; frontend-builder step confirmed nothing to implement (no-op); security-APPROVED (directory absent, zero code change, no dangling reference, live route intact, no security surface); QA-APPROVED (directory absent, no dangling refs, live route intact, lint clean, tsc exit 0, full suite 9 files/153 tests pass, zero TD-11 diff); release GO, classification **DONE (already-resolved; no code change; no PR)** — explicitly NOT "LOCAL DONE / PENDING PR" because there is no code artifact/diff to merge. Unlike ARCH-2 (implemented + merged via PR #4), TD-11 is DONE by virtue of being already-resolved/no-op, so it folds into the plain DONE count rather than a qualifier line. TD-11 produced NO follow-up FLAGs of its own and has no downstream dependents to unblock (the live reports route is independent of the removed component dir). Net effect on plain counts vs the prior run: READY 2→1 (TD-11 out), DONE 1→2 (TD-11 in: ARCH-2, TD-11); section/total counts unchanged at 33 (TD-11 already existed as an item; no new backlog items — TD-11 produced no FLAGs). **TD-13 (2026-06-26) is now LOCAL DONE / PENDING PR** — the Register #23 redundant-trigger-function cleanup is a real DB migration: NEW `supabase/migrations/0075_consolidate_updated_at_trigger_functions.sql` consolidates the two byte-identical `updated_at` trigger functions by KEEPING the canonical `set_updated_at()` (created 0002, already wired to ~9 BEFORE UPDATE triggers) and DROPPING the redundant `touch_updated_at()` (created 0033). feature-planner confirmed the two functions are byte-identical (`new.updated_at = now(); return new;`, plpgsql, neither SECURITY DEFINER nor sets search_path) and that `touch_updated_at()` has exactly ONE dependent — the `ai_prompts_updated_at` trigger on `ai_prompts` — while `set_updated_at()` backs ~9–10; recommended repointing that one trigger and dropping the duplicate. The migration REPOINTS `ai_prompts_updated_at` onto `set_updated_at()` FIRST (DROP+CREATE, faithful to the 0033 original — BEFORE UPDATE, FOR EACH ROW, no WHEN clause), then drops `touch_updated_at()`; NO CASCADE (CASCADE would silently drop the trigger and remove the auto-touch behavior), transactional, `IF EXISTS` for idempotency, `set_updated_at()` and its ~9 existing triggers deliberately untouched; no app/type change. Builder-complete, security-APPROVED (5/5 — functions identical and both SECURITY INVOKER so no privilege change, faithful trigger recreation, repoint-before-drop ordering, no CASCADE, no other dependent, no RLS/secret impact, scope clean), QA-APPROVED (10/10 — order safe, trigger diff vs 0033 identical except function name, bodies equivalent, no lost trigger, `set_updated_at()`'s existing triggers untouched, 0075 correct next number, no app/type change, lint clean, tsc exit 0, full suite 9 files/153 tests pass, scope clean; no migration-test harness so none invented), release GO; no commit/PR yet, release-authorized for a `fix/*` PR (recommended branch `fix/td-13-consolidate-updated-at-trigger-functions`); migration NOT yet applied (devops applies at deploy after 0074; non-prod sanity check worthwhile; no type regen needed). Following the ARCH-1/FUT-1/SEC-5/SEC-4/SEC-2/SEC-6/FUT-3/PERF-2/TD-3/TD-8/TD-9/TD-10 convention, the `LOCAL DONE / PENDING PR` qualifier is tracked separately from the plain statuses (so TD-13 leaves the READY tally and is listed on its own qualifier line rather than under READY/DONE). TD-13 produced NO follow-up FLAGs of its own and has no downstream dependents to unblock. **TD-13 was the LAST remaining plain-READY backlog item — after this closure the READY tier is EMPTY** (all READY items have been worked through this session). Net effect on plain counts vs the prior run: READY 1→0 (TD-13 out to its qualifier line — READY tier now empty), LOCAL DONE / PENDING PR 12→13 (TD-13 in: ARCH-1, FUT-1, SEC-5, SEC-4, SEC-2, SEC-6, FUT-3, PERF-2, TD-3, TD-8, TD-9, TD-10, TD-13); section/total counts unchanged at 33 (TD-13 already existed as an item; no new backlog items — TD-13 produced no FLAGs).)

> **Merge reconciliation (2026-06-26):** The 11 `LOCAL DONE / PENDING PR` items from this session — SEC-2, FUT-3, TD-8, TD-9, TD-10, TD-13, SEC-4, SEC-5, SEC-6, PERF-2, TD-3 — were landed on `main` as separate merge-commit PRs **#8–#18** (no CI exists; local lint + tsc + tests were green before each merge; migrations merged in ascending order 0070→0075). The two stale `LOCAL DONE / PENDING PR` rows ARCH-1 and FUT-1 (already merged earlier as **PR #6 / PR #7**) were reconciled to MERGED at the same time. PR→SHA: SEC-2 #8 (18101e9), FUT-3 #9 (bfc85c7), TD-8 #10 (64ceb7f), TD-9 #11 (fecacf4), TD-10 #12 (05555b1), TD-13 #13 (050b4ef), SEC-4 #14 (f2eb964), SEC-5 #15 (8906ca2), SEC-6 #16 (5a88351), PERF-2 #17 (4d61dbd), TD-3 #18 (f45fd98). This empties the `LOCAL DONE / PENDING PR` bucket (13→0); those 13 items are now counted under a clearly-labeled **MERGED 13** tally (merged-to-`main` is completion). **Migrations 0070–0075 are MERGED TO GIT but NOT YET APPLIED** to the linked DB — devops applies them at deploy in ascending order; the "migration not yet applied" caveat is preserved on each migration row. **TD-14 stays BLOCKED**: its `supabase gen types` typegen + close remain gated on the SEC-2 migration (0070) being APPLIED, not merely merged (the `README.md:186` doc fix can still proceed independently). The audit-only trio ARCH-3 / SEC-1 / SEC-3 is unchanged (`LOCAL DONE / no-PR (audit-only)` — their deliverables are `.claude/audits` artifacts deliberately excluded from the code PRs). Item total is unchanged at **33** (only statuses changed; nothing added or removed).

> Note on DONE: DONE is now **2** — **ARCH-2** (the first backlog work item evidenced as finished: all sub-tasks T1.1–T3.2 complete; implementation MERGED via PR #4 (`e35ef01`); both Phase-3 verification passes security-APPROVED + QA-APPROVED + release GO) and **TD-11** (already-resolved/verify-only no-op: the orphaned empty `components/reports/` directory is confirmed ABSENT on disk, no code change and no PR — DONE by virtue of being already-resolved, not by merge). The two DONE items thus reach DONE by different paths: ARCH-2 implemented + merged, TD-11 already-resolved/no-op. The Database Knowledge docs are complete but are captured implicitly as the provenance of this backlog rather than as a counted work item.
